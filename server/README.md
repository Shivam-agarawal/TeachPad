# TeachPad Server (Reserved Stub)

TeachPad v1 is a **100% local-first, offline-first application**. 
All whiteboard data, canvas rendering, and exports execute entirely in the user's browser client (`client/`) using HTML5 Canvas, IndexedDB, and the File System Access API.

This directory is reserved for potential future cloud synchronization or collaborative back-end services.
